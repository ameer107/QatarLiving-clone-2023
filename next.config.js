/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "files.qatarliving.com",
            "tpc.googlesyndication.com",
        ],

    },
}

module.exports = nextConfig
